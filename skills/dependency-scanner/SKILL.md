---
name: dependency-scanner
description: Use when scanning project dependencies for known vulnerabilities, outdated packages, or security advisories. Runs automated dependency audit across npm, pip, and other package managers.
---
# Dependency Scanner

Run `dependency_scanner.py` to audit project dependencies for vulnerabilities.

```bash
python dependency_scanner.py
```
